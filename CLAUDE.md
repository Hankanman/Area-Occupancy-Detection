# CLAUDE.md

This project's agent instructions live in [`AGENTS.md`](AGENTS.md), the agent-agnostic convention followed by Claude Code, Cursor, Codex, and other coding agents. Claude Code reads this file automatically, so it's kept as a pointer rather than a duplicate — read `AGENTS.md` for everything else.
